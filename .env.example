# Local dev — leave MCP_TRANSPORT unset (defaults to stdio)
# MCP_TRANSPORT=http

# Cloud/prod — set this to http
# MCP_TRANSPORT=http
# PORT=3000

# Auth token for cloud deployments (generate a strong random string)
# MCP_AUTH_TOKEN=your-secret-token-here
